{
	"name": "{{ name }}",
	"version": "{{ version }}",
	"author": [
		"{{ author }}"
	],
	"url": "{{ url }}",
	"descriptionmsg": "{{ lowername }}-desc",
	"license-name": "{{ license }}",
	"type": "other",
	"manifest_version": 1
	"MessagesDirs": {
		"{{ name }}": [
			"i18n"
		]
	},
{% if parts.javascript %}
	"ResourceModules": {
		"ext.{{ lowername }}": {
			"scripts": [
				"modules/ext.{{ lowername }}.js"
			],
			"styles": [
				"modules/ext.{{ lowername }}.css"
			],
			"messages": [],
			"dependencies": []
		},
		"ext.{{ lowername }}.tests": {
			"scripts": [
				"tests/ext.{{ lowername }}.test.js"
			],
			"dependencies": [
				"ext.{{ lowername }}"
			]
		},
	},
{% endif %}
{% if parts.specialpage %}
	"SpecialPages": {
		"{{ specialpage.name.noNamespace }}": "{{ specialname.className }}"
	},
	"ExtensionMessagesFiles": {
		"{{ name }}Alias": "{{ name }}.alias.php"
	},
{% endif %}
	"ResourceFileModulePaths": {
		"localBasePath": "",
		"remoteExtPath": "{{ name }}"
	},
}
