	/**
	 * Used to modify the edit buttons on the edit form
	 *
	 * @see https://www.mediawiki.org/wiki/Manual:Hooks/EditPageBeforeEditButtons
	 *
	 * @param EditPage &$editpage EditPage instance
	 * @param array &$buttons  the edit buttons found below the editing box ("Save", "Preview" and "Diff")
	 * @param {[type]} &$tabindex HTML tabindex of the last edit check/button
	 */
	public static function onEditPageBeforeEditButtons( &$editpage, &$buttons, &$tabindex ) {
		/* Your code here */
	}
