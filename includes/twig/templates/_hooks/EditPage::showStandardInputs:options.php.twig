	/**
	 * Allows injection of form fields into the editOptions area
	 *
	 * @see https://www.mediawiki.org/wiki/Manual:Hooks/EditPage::showStandardInputs:options
	 *
	 * @param EditPage $editor EditPage instance
	 * @param OutputPage $out OutputPage instance to write to
	 * @param {[type]} &$tabindex HTML tabindex of the last edit
	 *  check/button. Should be preincremented if necessary.
	 * @return true
	 */
	public static function onEditPage::showStandardInputs:options( $editor, $out, &$tabindex ) {
		/* Your code here */
	}
