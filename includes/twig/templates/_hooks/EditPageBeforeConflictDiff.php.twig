	/**
	 * Allows modifying the EditPage object and output when there's
	 * an edit conflict.
	 *
	 * @see https://www.mediawiki.org/wiki/Manual:Hooks/EditPageBeforeConflictDiff
	 *
	 * @param EditPage &$editor EditPage instance
	 * @param OutputPage &$out OutputPage instance to write to
	 * @return boolean Return false to halt normal diff output;
	 *  in this case you're responsible for computing and outputting
	 *  the entire "conflict" part, i.e., the "difference between
	 *  revisions" and "your text" headers and sections.
	 */
	public static function onEditPageBeforeConflictDiff( &$editor, &$out ) {
		/* Your code here */
	}
