	/**
	 * Allows modifying the edit checks below the textarea in the edit form
	 *
	 * @see https://www.mediawiki.org/wiki/Manual:Hooks/EditPageBeforeEditChecks
	 *
	 * @param EditPage &$editpage EditPage instance
	 * @param array &$checks Array of edit checks like "watch this page"/"minor edit"
	 * @param {[type]} &$tabindex HTML tabindex of the last edit check/button
	 */
	public static function onEditPageBeforeEditChecks( &$editpage, &$checks, &$tabindex ) {
		/* Your code here */
	}
