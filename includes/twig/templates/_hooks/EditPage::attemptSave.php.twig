	/**
	 * Called before an article is saved, that is at
	 * the beginning of internalAttemptSave() is called.
	 *
	 * @see https://www.mediawiki.org/wiki/Manual:Hooks/EditPage::attemptSave
	 *
	 * @param EditPage $editpage The current EditPage object
	 * @return boolean
	 */
	public static function onEditPageAttemptSave( $editpage ) {
		/* Your code here */
	}
