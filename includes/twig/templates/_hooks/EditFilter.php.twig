	/**
	 * Performs checks on an edit.
	 *
	 * @see https://www.mediawiki.org/wiki/Manual:Hooks/EditFilter
	 *
	 * @param EditPage $editor EditPage instance
	 * @param string $text Contents of the edit box
	 * @param string $section Section being edited
	 * @param string &$error Error message to return
	 * @param string $summary edit summary provided for edit
	 * @return boolean Return false to halt editing. You'll need
	 *  to handle error messages, etc. yourself.
	 *  Return true and modify $error (set to a value other than '').
	 *  This will cause the page to stay in edit mode and will cause
	 *  the contents of $error to be displayed above the edit box.
	 *  Note: $error should be wikitext.
	 */
	public static function onEditFilter( $editor, $text, $section, &$error, $summary ) {
		/* Your code here */
	}
