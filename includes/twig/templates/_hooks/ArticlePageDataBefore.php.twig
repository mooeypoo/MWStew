	/**
	 * Executes before data is loaded for the article requested
	 * As MediaWiki reads in the page data, this hook allows
	 * you to read more data fields related to the same database
	 * row than what is specified in WikiPage::selectFields().
	 * You specify more data fields by adding them to $fields
	 * parameter. Such data fields can then be manipulated from
	 * ArticlePageDataAfter.
	 *
	 * @see https://www.mediawiki.org/wiki/Manual:Hooks/ArticlePageDataBefore
	 *
	 * @param WikiPage $article WikiPage object requested
	 * @param array $fields fields to load from the database
	 * @return {[type]} [description]
	 */
	public static function onArticlePageDataBefore( $article, $fields ) {
		/* Your code here */
	}
