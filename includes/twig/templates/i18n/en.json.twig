{
{% if specialpage %}
	"{{ special.name.lowerFull }}-title": "{{ specialpage.title }}",
	"{{ special.name.lowerFull }}-intro": "{{ specialpage.intro }}",
{% endif %}
	"{{ lowername }}": "{{ fullName }}",
	"{{ lowername }}-desc": "{{ desc }}"
}
