<?php
/**
 * {{ specialpage.name.noNamespace }} SpecialPage for {{ name }} extension
 *
 * @file
 * @ingroup Extensions
 */
class {{ specialpage.className }} extends SpecialPage {
	public function __construct() {
		parent::__construct( '{{ specialpage.name.noNamespace }}' );
	}

	/**
	 * Show the page to the user
	 *
	 * @param string $sub The subpage string argument (if any).
	 */
	public function execute( $sub ) {
		$out = $this->getOutput();
		$out->setPageTitle( $this->msg( '{{ specialpage.name.i18nKey }}-title' ) );
		$out->addHelpLink( 'How to become a MediaWiki hacker' );
		$out->addWikiMsg( '{{ specialpage.name.i18nKey }}-intro' );
	}

	protected function getGroupName() {
		return 'other';
	}
}
