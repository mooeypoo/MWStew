matrix:
  include:
    - language: php
      php: 7.4

      before_script:
        - composer self-update
        - composer install --prefer-source --no-interaction

      script:
        - composer validate
        - composer test

    - language: node_js
      node_js: 14

      script:
        - npm test
